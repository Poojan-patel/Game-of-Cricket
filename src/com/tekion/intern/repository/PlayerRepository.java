package com.tekion.intern.repository;

import com.tekion.intern.dbconnector.MySqlConnector;

import java.sql.Connection;
import java.sql.PreparedStatement;

public class PlayerRepository {
//    public static int getPlayerIdByOffset(int playerOffset, int teamId){
//        Connection con = MySqlConnector.getConnection();
//        PreparedStatement ps = con.prepareStatement("select player_id from Player where team = ? limit ?, ?");
//        ps.setInt(1, teamId);
//        ps.setInt(2,playerOffset+1);
//        ps.setInt(3,playerOffset+1);
//    }
}
