select * from (select * from Player where player_order in (?,?) and team_id = ?) Player left outer join (select count(distinct ball_number) Balls, sum(score) Score, batsman from BallEvents where batsman in (?,?) and match_id = ? and batting_team = ? group by batsman) as ScoreCard on Player.player_order = ScoreCard.batsman